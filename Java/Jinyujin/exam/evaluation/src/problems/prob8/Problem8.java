package problems.prob8;

public class Problem8 {
    public static void main(String[] args) {
        // 이론 문제입니다.
        // 1. 상수화
        // 상수로 고정을 시킨 후 코드를 진행하면
        // 나중에 그 값이 바뀐다 하더라도 값만 바꿔주면 코드에서 그대로 적용되기 때문에 편리하다
        // 2. 불변 객체
        // final 선언한 변수에는 새로운 값을 대입하거나 덧셈, 뺄셈이 불가능하다
        // 입력되는 값을 변경하지 못하게 하여 동작의 무결성을 보장한다
        // 마음대로 값을 바꾸지 못히게 할 수 있다는 것이다
    }
}
