package com.example.demo.test.test10.repository;

import com.example.demo.test.test10.entity.AccountWithBusinessNumber;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TestAccountWithBusinessNumberRepository extends JpaRepository<AccountWithBusinessNumber, Long> {
}
