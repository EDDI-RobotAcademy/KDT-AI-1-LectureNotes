
server:
  port: 7777

spring:
  datasource: #aws에 올릴때는 가리는 것이 좋음
    url: jdbc:mysql://localhost:3306/jpa_test?allowpublickeyretrieval=true&usessl=false&serverTimezone=UTC
    #          프로젝트하면 바꾸게 될것1 jpa_test
    username: eddi
    password: eddi@123
    driver-class-name: com.mysql.cj.jdbc.Driver
    dialect: org.hibernate.dialect.MySQL8Dialect
  #DB id, pw도 보안을 위해 프로퍼티에 넣어줌
  jpa:
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
    database: mysql
#    프로젝트하면 바꾸게 될것2 mysql
    show-sql: true
    generate-ddl: true
    hibernate:
#      ddl-auto: create
      ddl-auto: update
#      ddl-auto => db를 자동으로 생성해줌

  cache:
    type: redis

  redis:
    host: 127.0.0.1
    port: 6379
    username: default
    password: eddi@123
